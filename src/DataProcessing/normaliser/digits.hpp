#ifndef DIGITS_HPP
#define DIGITS_HPP

#include <string>
#include <vector>

class DigitNormaliser {
    public:
    std::vector<std::string> normaliseDigits(const std::string& input, bool debug = false);
};

#endif // DIGITS_HPP